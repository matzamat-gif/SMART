// Virtual route for the center Add button
// This file exists to prevent Expo Router warnings
// Actual navigation is handled by the tab press listener in _layout.tsx
import { View } from 'react-native';

export default function ScanProxy() {
  return <View />;
}
